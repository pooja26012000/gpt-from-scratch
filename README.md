# GPT From Scratch

A GPT-style transformer built completely from scratch in PyTorch — no Hugging Face, no pre-built transformer layers.

## Status
🚧 Work in progress — following a step-by-step build process.

## Setup
See `requirements.txt` for dependencies.